package queue

// buffer.go now contains only helper behavior for buffer pooling used by
// the WAL and in-memory queue. The sharedBuf type was relocated to
// types.go to centralize types for the package.
