# Local template for act secrets (DO NOT COMMIT real secrets).
# Copy this to .secrets and fill values before running `act`.

NPM_TOKEN=
PYPI_API_TOKEN=
DOCKERHUB_USERNAME=
DOCKERHUB_PASSWORD=

# Optional: supply a test GITHUB_TOKEN if a workflow step expects it.
GITHUB_TOKEN=

